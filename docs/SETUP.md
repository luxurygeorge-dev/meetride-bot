# Установка и настройка MeetRide Bot

Подробная инструкция по установке и настройке системы MeetRide Bot с интеграцией Bitrix24.

---

## 📋 Требования

### Системные требования

- **ОС:** Linux (Ubuntu 18.04+ или Debian 10+)
- **PHP:** 7.4 или выше
- **Python:** 3.8 или выше
- **Composer:** последняя версия
- **Git:** для клонирования репозитория
- **Cron:** для планировщика напоминаний

### Доступы

- ✅ Доступ к серверу (SSH)
- ✅ Токены Telegram ботов (2 шт.)
- ✅ Доступ к Bitrix24 CRM
- ✅ Webhook URL от Bitrix24

---

## 🚀 Установка

### Шаг 1: Клонирование репозитория

```bash
cd /root
git clone https://github.com/luxurygeorge-dev/meetride-bot.git meetride
cd meetride
```

### Шаг 2: Установка зависимостей

#### PHP зависимости

```bash
# Установка Composer (если не установлен)
curl -sS https://getcomposer.org/installer | php
mv composer.phar /usr/local/bin/composer

# Установка зависимостей
cd /root/meetride
composer install
```

Будут установлены:
- `longman/telegram-bot` - PHP SDK для Telegram
- `telegram-bot/api` - Дополнительный SDK
- `irazasyed/telegram-bot-sdk` - Альтернативный SDK
- `carbon` - Работа с датами
- `guzzle` - HTTP клиент

#### Python зависимости

```bash
# Установка pip3 (если не установлен)
apt-get update
apt-get install python3-pip -y

# Установка зависимостей
pip3 install -r requirements.txt
```

Будут установлены:
- `pyTelegramBotAPI==4.12.0` - Python SDK для Telegram
- `requests==2.31.0` - HTTP клиент
- `dateparser==1.1.8` - Парсинг дат

### Шаг 3: Настройка конфигурации

#### Создание config.php

```bash
cp config/config.example.php config/config.php
nano config/config.php
```

**Заполните следующие параметры:**

```php
<?php
return [
    // Telegram Bot Configuration
    'telegram' => [
        'bot_token' => '7529690360:AAHED5aKmuKjjfFQPRI-0RQ8DlxlZARA2O4',
        'production_chat_id' => '-1001234567890', // ID общего чата водителей
        'test_chat_id' => '-1009876543210',       // ID тестового чата
        'environment' => 'production', // production или test
    ],

    // Bitrix24 Configuration
    'bitrix24' => [
        'domain' => 'meetride.bitrix24.ru',
        'webhook_url' => 'https://meetride.bitrix24.ru/rest/9/oo1pdplpuoy0q9ur/',
        'default_driver_contact_id' => 39,
    ],
];
```

#### Настройка токенов в файлах

**telegram_to_bitrix.py:**

```python
# Строка 6-7
TELEGRAM_BOT_TOKEN = '7992462078:AAGJ46crBdOMSAuIfWncFd0AEjrDiT4Tnww'
BITRIX24_WEBHOOK = 'https://meetride.bitrix24.ru/rest/9/oo1pdplpuoy0q9ur/crm.deal.add.json'
```

**botManager.php:**

```php
// Проверьте константы в начале файла (строки 16-37)
// Они должны соответствовать вашим полям в Bitrix24
```

---

## 🔗 Настройка Bitrix24

### 1. Создание полей в CRM

Перейдите в **Bitrix24 → Настройки → CRM → Настройка → Сделки → Добавить поле**

#### Основные поля (если не созданы):

| Название поля | Тип | Символьный код |
|---------------|-----|----------------|
| Водитель | Контакт | `UF_CRM_1751272181` |
| Telegram ID водителя | Строка | `UF_CRM_1751185017761` |
| Адрес подачи | Строка | `UF_CRM_1751269147414` |
| Адрес назначения | Строка | `UF_CRM_1751269175432` |
| Время поездки | Дата/Время | `UF_CRM_1751269222959` |
| Класс автомобиля | Список | `UF_CRM_1751271728682` |
| Пассажиры | Текст | `UF_CRM_1751271798896` |
| Дополнительные условия | Текст | `UF_CRM_1751269256380` |
| Промежуточные точки | Строка | `UF_CRM_1751822573510` |
| Номер рейса | Строка | `UF_CRM_1751271774391` |
| Сумма водителю | Число | `UF_CRM_1751271862251` |

#### SERVICE поля (для отслеживания изменений):

| Название поля | Тип | Символьный код |
|---------------|-----|----------------|
| Адрес подачи (служ.) | Строка | `UF_CRM_1751638512` |
| Адрес назначения (служ.) | Строка | `UF_CRM_1751638529` |
| Время поездки (служ.) | Строка | `UF_CRM_1751638617` |
| Пассажиры (служ.) | Текст | `UF_CRM_1759653762` |
| Промежуточные точки (служ.) | Строка | `UF_CRM_1754228146` |
| Класс автомобиля (служ.) | Строка | `UF_CRM_1759653779` |
| Номер заявки (служ.) | Строка | `UF_CRM_1759847340` |

### 2. Настройка списка классов автомобилей

Поле "Класс автомобиля" должно содержать:

| Название | ID |
|----------|-----|
| Эконом | 97 |
| Комфорт | 95 |
| Комфорт+ | 103 |
| Бизнес | 105 |
| Минивэн | 99 |
| Минивэн VIP | 101 |
| Стандарт | 119 |

### 3. Настройка стадий сделки

Убедитесь что у вас есть следующие стадии:

| ID стадии | Название |
|-----------|----------|
| `NEW` | Новая |
| `PREPARATION` | Поиск водителя |
| `PREPAYMENT_INVOICE` | Водитель принял |
| `EXECUTING` | Заявка выполняется |
| `FINAL_INVOICE` | Завершена |

### 4. Настройка Webhook

Подробная инструкция в [BITRIX24_INTEGRATION.md](BITRIX24_INTEGRATION.md)

**Краткая версия:**

1. Перейдите: **Настройки → Разработчикам → Вебхуки → Исходящие вебхуки**
2. Добавьте новый вебхук:
   - **URL:** `https://skysoft24.ru/meetRiedeBot/index.php`
   - **Событие:** `ONCRMDEALUPDATE` (При обновлении сделки)
   - **Фильтр:** `STAGE_ID = PREPARATION`
   - **Handler ID:** `3`

---

## 🌐 Настройка веб-сервера

### Nginx конфигурация

```nginx
server {
    listen 80;
    server_name skysoft24.ru;
    root /var/www/html;

    location /meetRiedeBot/ {
        try_files $uri $uri/ /meetRiedeBot/index.php?$query_string;
    }

    location ~ \.php$ {
        fastcgi_pass unix:/var/run/php/php7.4-fpm.sock;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    }
}
```

### Apache конфигурация

```apache
<VirtualHost *:80>
    ServerName skysoft24.ru
    DocumentRoot /var/www/html

    <Directory /var/www/html/meetRiedeBot>
        AllowOverride All
        Require all granted
    </Directory>
</VirtualHost>
```

### Права доступа

```bash
# Права на папку с webhook
chown -R www-data:www-data /var/www/html/meetRiedeBot
chmod -R 755 /var/www/html/meetRiedeBot

# Права на папку с логами
mkdir -p /var/www/html/meetRiedeBot/logs
chmod 777 /var/www/html/meetRiedeBot/logs
```

---

## ⏰ Настройка Cron

### Установка cron задач

```bash
cd /root/meetride
./scripts/setup_cron.sh
```

Или вручную:

```bash
crontab -e
```

Добавьте:

```cron
# Система напоминаний (каждые 5 минут)
*/5 * * * * cd /root/meetride && php reminder_scheduler_working.php >> /root/meetride/logs/cron.log 2>&1
```

### Проверка cron

```bash
# Просмотр задач
crontab -l

# Проверка логов
tail -f /root/meetride/logs/cron.log
```

---

## 🤖 Запуск ботов

### Бот создания заявок (telegram_to_bitrix.py)

```bash
# Запуск в фоне
cd /opt/meetride
nohup python3 telegram_to_bitrix.py > /dev/null 2>&1 &

# Проверка что запустился
ps aux | grep telegram_to_bitrix
```

### Бот уведомлений (botManager.php)

Этот бот работает через webhook, запускать не нужно. Проверьте что endpoint доступен:

```bash
curl https://skysoft24.ru/meetRiedeBot/index.php
# Ожидаемый ответ: OK - Webhook received
```

---

## 🔍 Проверка установки

### 1. Проверка PHP

```bash
cd /root/meetride
php simple_test.php
```

Должно вывести:
```
✅ Все методы проверены успешно
✅ Все константы проверены успешно
✅ CRest работает
```

### 2. Проверка webhook

```bash
curl -X POST https://skysoft24.ru/meetRiedeBot/index.php \
  -d "event=ONCRMDEALUPDATE" \
  -d "data[FIELDS][ID]=123"
```

### 3. Проверка cron

```bash
# Ручной запуск
cd /root/meetride
php reminder_scheduler_working.php

# Должно вывести:
# ✅ Система напоминаний выполнена!
```

### 4. Создание тестовой заявки

Отправьте в бота сообщение:

```
#️⃣ TEST-1234
🚗 Комфорт
📆 завтра 15:00
🅰️ Москва, Красная площадь
🅱️ Шереметьево
👥 Тестов Тест (+79001112233)
💰 5000/4000
```

Проверьте что:
- ✅ Заявка создалась в Bitrix24
- ✅ Сообщение пришло в Telegram чат
- ✅ Есть кнопки [✅ Принять] [❌ Отказаться]

---

## 📂 Развертывание на продакшен

### Копирование файлов

```bash
# Webhook
cp -r /root/meetride/* /var/www/html/meetRiedeBot/

# Бот создания заявок
cp /root/meetride/telegram_to_bitrix.py /opt/meetride/

# Перезапуск бота
killall python3
cd /opt/meetride
nohup python3 telegram_to_bitrix.py > /dev/null 2>&1 &
```

---

## 🐛 Решение проблем

### Ошибка: "composer: command not found"

```bash
curl -sS https://getcomposer.org/installer | php
mv composer.phar /usr/local/bin/composer
chmod +x /usr/local/bin/composer
```

### Ошибка: "Class 'CRest' not found"

```bash
# Проверьте путь к CRest
ls -la /home/telegramBot/crest/crest.php

# Если файл отсутствует, скопируйте из src/crest/
cp /root/meetride/src/crest/crest.php /home/telegramBot/crest/
```

### Ошибка: "Failed to open stream: Permission denied"

```bash
# Дайте права на логи
chmod 777 /root/meetride/logs/
chmod 666 /root/meetride/logs/*.log
```

---

## ✅ Чек-лист готовности

- [ ] PHP 7.4+ установлен
- [ ] Python 3.8+ установлен
- [ ] Composer зависимости установлены
- [ ] Python зависимости установлены
- [ ] Токены настроены в config.php
- [ ] Поля созданы в Bitrix24
- [ ] Webhook настроен в Bitrix24
- [ ] Веб-сервер настроен
- [ ] Cron задачи настроены
- [ ] Бот создания заявок запущен
- [ ] Webhook endpoint доступен
- [ ] Тестовая заявка создается

---

**Следующий шаг:** [TESTING.md](TESTING.md) - Тестирование системы

**Вернуться к:** [README.md](../README.md) - Главная документация

