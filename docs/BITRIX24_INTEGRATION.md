# Интеграция с Bitrix24

Подробная инструкция по настройке интеграции MeetRide Bot с Bitrix24 CRM.

---

## 📋 Содержание

- [Настройка webhook](#настройка-webhook)
- [Конфигурация полей CRM](#конфигурация-полей-crm)
- [Настройка стадий сделки](#настройка-стадий-сделки)
- [Исходящие вебхуки](#исходящие-вебхуки)
- [REST API доступ](#rest-api-доступ)
- [Тестирование интеграции](#тестирование-интеграции)

---

## 🔗 Настройка webhook

### Получение Webhook URL

1. Откройте Bitrix24: https://meetride.bitrix24.ru/
2. Перейдите: **Настройки → Разработчикам → Вебхуки → Входящий вебхук**
3. Нажмите **"Создать входящий вебхук"**
4. Выберите права доступа:
   - ✅ **CRM** (все права)
   - ✅ **Контакты** (чтение, запись)
   - ✅ **Сделки** (чтение, запись, удаление)
5. Скопируйте URL вида:
   ```
   https://meetride.bitrix24.ru/rest/9/oo1pdplpuoy0q9ur/
   ```

### Настройка в коде

Вставьте webhook URL в файлы:

**telegram_to_bitrix.py:**

```python
BITRIX24_WEBHOOK = 'https://meetride.bitrix24.ru/rest/9/oo1pdplpuoy0q9ur/crm.deal.add.json'
```

**Проверка подключения:**

```bash
cd /root/meetride
php -r "
require_once('/home/telegramBot/crest/crest.php');
\$result = CRest::call('crm.deal.list', ['select' => ['ID', 'TITLE']]);
echo 'Подключение OK! Найдено сделок: ' . count(\$result['result']) . PHP_EOL;
"
```

---

## 📝 Конфигурация полей CRM

### Создание пользовательских полей

Перейдите: **Настройки → CRM → Настройка → Сделки → Пользовательские поля**

#### Основные поля заявки

| № | Название поля | Тип | Обязательное | Символьный код (примерный) |
|---|---------------|-----|--------------|----------------------------|
| 1 | Водитель | Контакт | Нет | `UF_CRM_1751272181` |
| 2 | Telegram ID водителя | Строка | Нет | `UF_CRM_1751185017761` |
| 3 | Адрес подачи | Строка | Да | `UF_CRM_1751269147414` |
| 4 | Адрес назначения | Строка | Да | `UF_CRM_1751269175432` |
| 5 | Время подачи | Дата/Время | Да | `UF_CRM_1751269222959` |
| 6 | Класс автомобиля | Список | Да | `UF_CRM_1751271728682` |
| 7 | Пассажиры | Текст | Нет | `UF_CRM_1751271798896` |
| 8 | Дополнительные условия | Текст | Нет | `UF_CRM_1751269256380` |
| 9 | Промежуточные точки | Строка | Нет | `UF_CRM_1751822573510` |
| 10 | Номер рейса | Строка | Нет | `UF_CRM_1751271774391` |
| 11 | Сумма клиента | Число | Нет | `UF_CRM_XXXXXXXXX` |
| 12 | Сумма водителю | Число | Нет | `UF_CRM_1751271862251` |

#### SERVICE поля (для отслеживания изменений)

| № | Название поля | Тип | Скрытое | Символьный код |
|---|---------------|-----|---------|----------------|
| 1 | Адрес подачи (служ.) | Строка | Да | `UF_CRM_1751638512` |
| 2 | Адрес назначения (служ.) | Строка | Да | `UF_CRM_1751638529` |
| 3 | Время подачи (служ.) | Строка | Да | `UF_CRM_1751638617` |
| 4 | Пассажиры (служ.) | Текст | Да | `UF_CRM_1759653762` |
| 5 | Промежуточные точки (служ.) | Строка | Да | `UF_CRM_1754228146` |
| 6 | Класс автомобиля (служ.) | Строка | Да | `UF_CRM_1759653779` |
| 7 | Номер заявки (служ.) | Строка | Да | `UF_CRM_1759847340` |
| 8 | Доп. условия (служ.) | Текст | Да | `UF_CRM_1758709126` |
| 9 | Сообщение в группу отправлено | Строка | Да | `UF_CRM_XXXXXXXXX` |

### Настройка списка "Класс автомобиля"

1. Создайте поле типа "Список"
2. Добавьте значения:

| Название | ID (внутренний) |
|----------|-----------------|
| Эконом | 97 |
| Комфорт | 95 |
| Комфорт+ | 103 |
| Бизнес | 105 |
| Минивэн | 99 |
| Минивэн VIP | 101 |
| Стандарт | 119 |

**Как узнать ID значений:**

```bash
cd /root/meetride
php -r "
require_once('/home/telegramBot/crest/crest.php');
\$fields = CRest::call('crm.deal.fields')['result'];
\$carClassField = \$fields['UF_CRM_1751271728682'];
print_r(\$carClassField['items']);
"
```

### Получение символьных кодов полей

```bash
cd /root/meetride
php -r "
require_once('/home/telegramBot/crest/crest.php');
\$fields = CRest::call('crm.deal.fields')['result'];
foreach (\$fields as \$key => \$field) {
    if (strpos(\$key, 'UF_CRM_') === 0) {
        echo \$key . ' = ' . \$field['formLabel'] . PHP_EOL;
    }
}
"
```

Скопируйте полученные коды и обновите константы в `botManager.php` (строки 16-37).

---

## 🚦 Настройка стадий сделки

### Создание стадий

Перейдите: **CRM → Настройки → Настройка статусов → Сделка**

Создайте следующие стадии (если их нет):

| № | ID стадии | Название | Цвет | Описание |
|---|-----------|----------|------|----------|
| 1 | `NEW` | Новая | Серый | Заявка только создана |
| 2 | `PREPARATION` | Поиск водителя | Синий | Автоматическая отправка в Telegram |
| 3 | `PREPAYMENT_INVOICE` | Водитель принял | Желтый | Водитель назначен, едет на подачу |
| 4 | `EXECUTING` | Заявка выполняется | Оранжевый | Водитель начал поездку |
| 5 | `FINAL_INVOICE` | Завершена | Зеленый | Поездка завершена успешно |
| 6 | `LOSE` | Отменена | Красный | Заявка отменена |

### Автоматический переход NEW → PREPARATION

Создайте бизнес-процесс (или триггер):

1. **Триггер:** При создании сделки
2. **Условие:** Стадия = NEW
3. **Действие:** Изменить стадию на PREPARATION

**Или настройте в автоматизации:**

- **CRM → Автоматизация → Сделки → Стадия "Новая"**
- Добавьте действие "Изменить стадию" → PREPARATION

---

## 📤 Исходящие вебхуки

### Webhook #1: Отправка в чат при PREPARATION

**Назначение:** При попадании сделки на стадию PREPARATION автоматически отправляется уведомление в Telegram чат.

#### Настройка

1. **Настройки → Разработчикам → Вебхуки → Исходящие вебхуки**
2. Нажмите **"Добавить вебхук"**
3. Заполните:
   - **Событие:** ONCRMDEALUPDATE
   - **Фильтр:**
     ```
     STAGE_ID = PREPARATION
     ```
   - **URL:** `https://skysoft24.ru/meetRiedeBot/src/index.php`
   - **Handler ID:** `3` (важно для предотвращения дублей!)

#### Параметры запроса

Bitrix24 будет отправлять POST запрос с данными:

```json
{
  "event": "ONCRMDEALUPDATE",
  "event_handler_id": "3",
  "data": {
    "FIELDS": {
      "ID": "123",
      "STAGE_ID": "PREPARATION",
      "TITLE": "Заявка: PT-1234"
    }
  },
  "ts": "1234567890",
  "auth": {
    "application_token": "ancn7qxhagfsifwxkcoo7s04tfc8q2ez"
  }
}
```

### Webhook #2: Ручное назначение водителя (опционально)

Если водитель назначается вручную в CRM:

1. **URL:** `https://skysoft24.ru/meetRiedeBot/webhook_stage3.php`
2. **Событие:** ONCRMDEALUPDATE
3. **Фильтр:**
   ```
   STAGE_ID = PREPAYMENT_INVOICE
   OLD.STAGE_ID != PREPAYMENT_INVOICE
   ```

---

## 🔐 REST API доступ

### CRest библиотека

Проект использует CRest для работы с Bitrix24 API.

**Расположение:** `/home/telegramBot/crest/crest.php`

### Основные методы API

#### Получение сделки

```php
$deal = CRest::call('crm.deal.get', [
    'id' => 123,
    'select' => ['ID', 'TITLE', 'STAGE_ID', 'UF_CRM_1751272181']
])['result'];
```

#### Обновление сделки

```php
CRest::call('crm.deal.update', [
    'id' => 123,
    'fields' => [
        'STAGE_ID' => 'PREPAYMENT_INVOICE',
        'UF_CRM_1751272181' => 456, // ID водителя
        'TITLE' => 'PT-1234 | Иванов И.И.'
    ]
]);
```

#### Список сделок

```php
$deals = CRest::call('crm.deal.list', [
    'filter' => [
        'STAGE_ID' => 'PREPAYMENT_INVOICE',
        '>UF_CRM_1751269222959' => date('Y-m-d H:i:s')
    ],
    'select' => ['ID', 'TITLE', 'UF_CRM_1751269222959']
])['result'];
```

#### Получение контакта

```php
$contact = CRest::call('crm.contact.get', [
    'id' => 456,
    'select' => ['ID', 'NAME', 'LAST_NAME', 'UF_CRM_1751185017761']
])['result'];
```

### Лимиты API

- **Запросов в секунду:** 2
- **Запросов в день:** 10,000
- **Пакетных операций:** до 50 за раз

**Рекомендация:** Используйте batch запросы для множественных операций:

```php
CRest::callBatch([
    'deal_get' => ['crm.deal.get', ['id' => 123]],
    'contact_get' => ['crm.contact.get', ['id' => 456]]
]);
```

---

## 🧪 Тестирование интеграции

### Тест 1: Проверка подключения

```bash
cd /root/meetride
php -r "
require_once('/home/telegramBot/crest/crest.php');
try {
    \$result = CRest::call('crm.deal.fields');
    echo '✅ Подключение к Bitrix24 работает!' . PHP_EOL;
    echo 'Найдено полей: ' . count(\$result['result']) . PHP_EOL;
} catch (Exception \$e) {
    echo '❌ Ошибка: ' . \$e->getMessage() . PHP_EOL;
}
"
```

### Тест 2: Создание тестовой заявки

```bash
php -r "
require_once('/home/telegramBot/crest/crest.php');
\$result = CRest::call('crm.deal.add', [
    'fields' => [
        'TITLE' => 'TEST-9999',
        'STAGE_ID' => 'NEW',
        'UF_CRM_1751269147414' => 'Москва, Тестовая',
        'UF_CRM_1751269175432' => 'Москва, Тестовая 2',
        'UF_CRM_1751269222959' => date('Y-m-d H:i:s', strtotime('+2 hours'))
    ]
]);
echo 'Создана заявка ID: ' . \$result['result'] . PHP_EOL;
"
```

### Тест 3: Проверка webhook endpoint

```bash
curl -X POST https://skysoft24.ru/meetRiedeBot/src/index.php \
  -H "Content-Type: application/json" \
  -d '{
    "event": "ONCRMDEALUPDATE",
    "event_handler_id": "3",
    "data": {
      "FIELDS": {
        "ID": "123"
      }
    }
  }'
```

**Ожидаемый ответ:**

```
OK - Webhook received
Processing deal update: 123
```

### Тест 4: Webhook в Bitrix24

1. Создайте тестовую сделку в Bitrix24
2. Переведите её на стадию PREPARATION
3. Проверьте логи:

```bash
tail -50 /var/www/html/meetRiedeBot/logs/webhook_debug.log
```

Должны быть записи:

```
2025-11-04 17:00:00 - ONCRMDEALUPDATE event detected (handler: 3)
2025-11-04 17:00:01 - Deal 123 is PREPARATION
2025-11-04 17:00:02 - Sending deal 123 to drivers
2025-11-04 17:00:03 - Deal 123 sent successfully
```

---

## 🐛 Решение проблем

### Ошибка: "Invalid webhook"

**Причина:** Неправильный webhook URL или истек срок действия

**Решение:**

1. Проверьте срок действия webhook в Bitrix24
2. Пересоздайте webhook если нужно
3. Обновите URL в коде

### Ошибка: "Field not found: UF_CRM_XXXXXX"

**Причина:** Поле не существует в CRM

**Решение:**

```bash
# Получите список всех полей
cd /root/meetride
php -r "
require_once('/home/telegramBot/crest/crest.php');
\$fields = CRest::call('crm.deal.fields')['result'];
foreach (\$fields as \$key => \$field) {
    if (strpos(\$key, 'UF_CRM_') === 0) {
        echo \$key . PHP_EOL;
    }
}
" | grep "UF_CRM_175"
```

Найдите нужное поле и обновите константу в `botManager.php`.

### Дублирование сообщений в чате

**Причина:** Несколько webhook'ов с одинаковым событием

**Решение:**

1. В Bitrix24 → Вебхуки → найдите все ONCRMDEALUPDATE
2. Удалите дубликаты
3. Оставьте только один с Handler ID = 3

### Webhook не срабатывает

**Причина:** Фильтр не проходит или endpoint недоступен

**Решение:**

```bash
# Проверьте доступность endpoint
curl https://skysoft24.ru/meetRiedeBot/src/index.php

# Проверьте логи webhook в Bitrix24
# Настройки → Разработчикам → Вебхуки → История вызовов
```

---

## 📊 Мониторинг интеграции

### Команды для проверки

```bash
# Проверка последних сделок
php -r "
require_once('/home/telegramBot/crest/crest.php');
\$deals = CRest::call('crm.deal.list', [
    'order' => ['ID' => 'DESC'],
    'select' => ['ID', 'TITLE', 'STAGE_ID'],
    'filter' => ['>=ID' => 1300]
])['result'];
foreach (\$deals as \$deal) {
    echo \$deal['ID'] . ' - ' . \$deal['TITLE'] . ' - ' . \$deal['STAGE_ID'] . PHP_EOL;
}
"

# Статистика по стадиям
php -r "
require_once('/home/telegramBot/crest/crest.php');
\$deals = CRest::call('crm.deal.list', ['select' => ['STAGE_ID']])['result'];
\$stats = [];
foreach (\$deals as \$deal) {
    \$stage = \$deal['STAGE_ID'] ?? 'UNKNOWN';
    \$stats[\$stage] = (\$stats[\$stage] ?? 0) + 1;
}
print_r(\$stats);
"
```

---

## 🔗 Полезные ссылки

- **Bitrix24 REST API документация:** https://dev.1c-bitrix.ru/rest_help/
- **Методы CRM:** https://dev.1c-bitrix.ru/rest_help/crm/
- **Вебхуки:** https://dev.1c-bitrix.ru/rest_help/general/event_bind.php
- **CRest на GitHub:** https://github.com/bitrix-tools/crest

---

**Вернуться к:** [README.md](../README.md) - Главная документация
**Смотрите также:** [SETUP.md](SETUP.md) - Установка и настройка

